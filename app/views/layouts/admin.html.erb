<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t('app.name') %><%= ":: #{yield(:title)}" unless yield(:title).blank? %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--><![if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "admin", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#"><%= t('app.name') %></a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li><%= link_to t('administration.sections.organizations'), admin_organizations_path  %></li>
              <li><%= link_to t('administration.sections.pollsters'), admin_pollsters_path  %></li>
              <li><%= link_to t('administration.sections.devices'), admin_devices_path  %></li>
              <li><%= link_to t('administration.sections.managers'), admin_managers_path  %></li>
              <li><%= link_to t('administration.sections.meta_surveys'), admin_meta_surveys_path  %></li>
              <li><%= link_to t('administration.sections.admins'), admin_admin_users_path, :class => "highlighted" %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span9">
					
					<%= render 'layouts/shared/messages' %>
					
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; <%= t('app.name') %> <%= Date.today.year %></p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
