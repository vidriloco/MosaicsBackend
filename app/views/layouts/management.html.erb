<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t('app.name') %><%= ":: #{yield(:title)}" unless yield(:title).blank? %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--><![if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "management", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
  </head>
  <body>

		<div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#"><%= t('app.name') %>&nbsp;&reg;</a>
          <div class="container-fluid nav-collapse">
            
						<% if manager_signed_in? %>
							<ul class="nav pull-right">
								<li><a href="#" class="organization"><%= current_manager.organization.name %></a></li>
								<li class="dropdown">
									<a class="dropdown-toggle" data-toggle="dropdown" href="#">
										<%= current_manager.username %>
										<b class="caret"></b>
									</a>
									<ul class="dropdown-menu">
										<li><a href="<%= destroy_manager_session_path %>" data-method="delete"><i class="icon-off"></i>&nbsp;<%= t('general.actions.logout') %></a></li>
									</ul>
								</li>
							</ul>
						<% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>


		
		<%= render 'layouts/shared/messages' %>
		
    <%= yield %>


    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>